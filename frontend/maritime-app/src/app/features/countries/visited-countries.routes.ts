import { Routes } from '@angular/router';
import { VisitedCountryListComponent } from './visited-countries-list/visited-countries-list.component';

export const COUNTRIES_ROUTES: Routes = [
  { path: '', component: VisitedCountryListComponent }
];
